# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是需要快速获得业务结论的运营人员和业务分析人员。他们通常从已有业务数据出发，通过自然语言提出问题、比较结果并继续追问，不应被 SQL、物理字段或运行时细节打断。

数据管理员是次要用户，负责导入或注册数据源、检查数据目录、确认语义字段映射并激活可查询的数据绑定。

## Product Purpose

Data Assistant 是一个受治理的自然语言数据分析工作台。它让用户上传或注册自己的数据，在明确的数据源和语义绑定范围内，通过自然语言获得分析结论、图表与可核查的查询结果。

成功意味着业务用户能够理解当前分析基于哪份数据、快速提出问题、读懂结论并自然地继续探索；数据管理员能够可靠地完成数据接入与激活。

## Positioning

产品的核心机制不是把自然语言直接转换为任意 SQL，而是把每次分析固定在已发现目录、已确认语义绑定、只读执行和版本锁定的数据范围内，并提供可取消、可回放、可追溯的运行过程。

## Operating Context

- 用户先登录租户工作区。
- 数据管理员上传 CSV、XLSX 或 SQLite 文件，或通过凭据引用注册 PostgreSQL 数据源。
- 系统发现数据目录并生成不可变只读快照。
- 数据管理员确认语义字段映射并激活绑定。
- 业务用户在会话中使用 Plan、Preview 或 Execute 模式提问。
- 系统通过流式事件返回分析状态，最终展示答案、图表、表格、逻辑计划、SQL、错误与待审批记忆提案。
- 会话、运行事件、数据绑定和记忆提案都按租户及用户身份隔离。

## Capabilities and Constraints

- 保留现有 FastAPI、React、Vite 和 TypeScript 技术栈。
- 保留现有后端 API 功能与权限边界，不为视觉方案虚构新能力。
- 核心分析支持 Plan、Preview、Execute 三种模式。
- 支持流式运行、主动取消和事件回放。
- 支持会话创建、重命名、归档式删除和历史消息读取。
- 数据源接入与语义配置应成为独立的分步流程；对话分析是主工作台。
- 允许重组导航、调整产品文案，并隐藏尚未实现的占位入口。
- 桌面端优先，但移动端必须完整可用，不得仅依赖固定侧栏或横向溢出。

## Brand Commitments

- 当前产品名为 “Data Assistant”。
- 面向主要用户的产品文案以简体中文为主；SQL、API、Plan、Preview、Execute 等必要技术术语应附有清晰解释，而不是无上下文混用中英文。
- 视觉方向应简约、清晰、具有可信赖的科技感，避免娱乐化和过度装饰。

## Evidence on Hand

- 产品架构与能力说明：`README.md`
- 后端路由与协议：`src/api/routes.py`、`src/api/schemas.py`、`docs/apifox-openapi.json`
- 当前前端实现：`frontend/src/App.tsx`、`frontend/src/DataSourcePanel.tsx`、`frontend/src/styles.css`
- 当前界面已通过本地演示环境实际登录并走查。
- 当前没有正式品牌标志、品牌字体、客户证明、性能基准或商业数据；后续设计不得虚构这些内容。

## Product Principles

1. 先让用户知道正在分析哪份数据，再让用户提问。
2. 将治理和技术复杂度渐进披露，不让它阻塞普通业务分析。
3. 每个状态都应清楚回答“发生了什么、下一步做什么、结果是否可信”。
4. 高频分析路径保持快速，低频数据配置路径保持可指导、可检查。
5. 桌面与移动端共享同一任务逻辑，布局可以自适应但能力不能缺失。

## Accessibility & Inclusion

界面应支持键盘操作、清晰焦点、足够对比度、语义化控件、非颜色唯一状态表达，并在窄屏和中文长文本下保持可读与可操作。
