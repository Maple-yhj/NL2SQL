# NL2SQL 实施计划

## 目标架构

用户选择数据源后，系统通过统一的数据源注册表完成目录发现和版本快照；经确认的语义绑定把物理字段映射为稳定语义，再由结构化 Planner、Query IR 和方言编译器生成只读查询，最终输出可追溯的答案、表格和图表。

首批数据源：PostgreSQL、SQLite、Excel（XLSX）和 CSV。文件数据源采用隔离快照，凭据、认证、会话与记忆保留在控制面数据库中。

## 实施阶段

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 0 | 跨平台测试、构建与发布门禁 | 已完成 |
| 1 | 数据源契约、注册表、目录与快照 | 已完成 |
| 2 | 现有 PostgreSQL 接入统一抽象 | 已完成 |
| 3 | SQLite 只读数据源 | 已完成 |
| 4 | Excel/CSV 快照与 DuckDB 查询 | 已完成 |
| 5 | 数据源 API、前端选择与语义绑定 | 已完成 |
| 6 | 可信分析结论、表格与安全图表 | 已完成 |
| 7 | SSE、取消/回放、记忆审批与通用部署 | 已完成 |
| 8 | 移除默认 OList 部署，改为仅由用户上传或注册数据源 | 已完成 |

## 实施原则

- 保留已完成的多模型 Provider 和 Planner 结构化输出修复。
- Planner 不直接依赖数据库方言或自由生成物理 SQL。
- 所有数据查询只读、受限、可取消，并绑定数据源和语义版本。
- 自动语义映射必须经确认后激活。
- 每完成一个阶段，同步更新本文件和对应测试。

当前产品入口使用无预置业务数据的上传运行时。用户必须先上传 CSV、XLSX、SQLite，或注册 PostgreSQL，再确认并激活语义绑定；OList 仅保留为显式兼容运行时与离线回归测试资产。
