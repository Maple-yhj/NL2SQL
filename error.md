# Olist 前端测试错误记录

- 测试时间：2026-08-11（Asia/Shanghai）
- 前端地址：`http://127.0.0.1:5173/`
- 租户/用户：`demo` / `admin`
- 数据源：`Olist 全量 CSV 20260808`
- 测试集：`/Users/yehj/project/olist/olist_agent_test_set.json`
- 执行范围：测试集推荐的 `smoke_15`（15 条）；每条均通过前端“新建分析”独立提交，使用“执行”模式
- 超时口径：通常等待 180 秒；`PROD_01` 单独等待 210 秒

登录、数据源加载、新建会话和问题输入均正常。15 条中仅 `META_04` 得到基本可用结果；3 条完成但答案错误或不完整；11 条未在超时前完成。

## 1. 澄清后恢复运行卡住（11 条）

共同复现步骤：

1. 在前端新建分析并输入测试问题。
2. Agent 完成 catalog/semantic 检查后提示需要确认。
3. 在“恢复运行所需输入”中明确同意拆分查询或补充计算口径，点击“继续分析”。
4. 页面重新显示“正在执行分析计划”，但计划长期停留在 `semantic.inspect` 或后续证据绑定阶段；查询/回答始终未完成。

| 用例 | 运行 ID | 超时前状态 |
| --- | --- | --- |
| `META_01` | `analysis-run-e7103eda7e754c3989e2a2453b10553a` | 已同意分别统计 9 个表；0 项证据，停在 `semantic.inspect` 后 |
| `META_06` | `analysis-run-f90d3ea817a74bec84701cc75dc22cfe` | 复测时明确要求 4 个独立空值查询；仍停在 `semantic.inspect` 后 |
| `META_10` | `analysis-run-e9decae1dbf842a9b2dfecd40a6ecac9` | 复测时明确接受订单级明细后再汇总；仍停在 `semantic.inspect` 后 |
| `FIN_03` | `analysis-run-9d339be60d744a03bdbd1bbafd85c13c` | 查询已返回 1 行并收集 1 项证据，但卡在 `evidence.collect`/最终证据绑定 |
| `FIN_09` | `analysis-run-e61e01a481e74e29bfc332d9952b209c` | 已同意先分别按 `order_id` 聚合再连接；0 项证据，停在 `semantic.inspect` 后 |
| `CUST_03` | `analysis-run-10ef96458741453d96ecbe282a1274b0` | 二次聚合澄清后，0 项证据，停在 `semantic.inspect` 后 |
| `PROD_01` | `analysis-run-79afcd12da8a4e66a30913e9612b6678` | 提示英文类目处于不可安全连接的独立组件；确认继续后仍停在 `semantic.inspect` 后 |
| `DELIV_01` | `analysis-run-354d2ed69afa4c24aaea4c49e465be04` | 已同意拆成总数和准时数两个查询；0 项证据，停在 `semantic.inspect` 后 |
| `DELIV_05` | `analysis-run-da38082773f44576b08cc148c1f6488b` | 同意按实际送达时间与预计时间派生准时/晚到；0 项证据，停在 `semantic.inspect` 后 |
| `REV_03` | `analysis-run-3c99b1eed082430fad0d2bff57e1ead6` | 同一“请明确分子、分母”澄清重复出现 3 次，未进入查询编译 |
| `REASON_04` | `analysis-run-e72a0f27a64f499cbf3f83b4bb232e28` | 确认泄漏/可用字段口径后，0 项证据，停在 `semantic.inspect` 后 |

原因判断（基于前端可见状态）：

- 查询计划能力不覆盖多字段独立过滤、二次聚合、条件比率、日期比较派生分组及部分跨组件连接。
- Agent 能识别限制并发起澄清，但恢复后没有把用户确认转换成新的可执行计划，状态机仍停留在原 `semantic.inspect` 步骤。
- `FIN_03` 是另一种卡住：查询已经执行并收集证据，但最终 claim/evidence 绑定未结束。
- 浏览器控制台在这些过程中没有捕获到 `error`/`warn` 日志，因此更像后端 Agent 编排/计划恢复问题，而不是前端 JavaScript 异常。

## 2. `TEMP_02` 按时间戳聚合，未按月聚合

- 问题：在 2017-01 至 2018-08 的完整月份窗口内，订单量最高的 5 个月是什么？
- 复测运行 ID：`analysis-run-4ed81c94b3a04a63973c03da1eeba91a`
- 澄清输入：明确要求“按 `order_purchase_timestamp` 的月份分组，统计 `COUNT(DISTINCT order_id)`，限定 2017-01 至 2018-08，并取最高 5 个月”。
- 实际结果：返回 5 个完整时间戳，每行 `order_count = 3`，并声称缺少月度证据。例如 `2018-02-19T15:37:47 -> 3`。
- 期望前 5 月：`2017-11=7544`、`2018-01=7269`、`2018-03=7211`、`2018-04=6939`、`2018-05=6873`。
- 原因判断：查询编译没有应用月份截断/月份分组，而是直接按原始时间戳分组；明确澄清没有覆盖错误的时间粒度。

## 3. `SELL_02` 最终回答与同页证据不一致，且结果字段不完整

- 运行 ID：`analysis-run-665b1cb29d71469688a080a475196255`
- 实际自然语言回答：第一名销售额为 `226,927.93`。
- 同页查询结果及期望值：第一名销售额为 `226,987.93`。
- 其他问题：测试集要求同时返回卖家城市、州、销量、订单数和销售额；页面只返回 `seller_id` 与销售额。
- 原因判断：回答生成阶段抄写/数字一致性校验缺失；查询结果维度也未覆盖测试集要求的完整字段。

## 4. `REASON_02` 将支付总额错误地定义为企业“总收入”

- 运行 ID：`analysis-run-a68c53d8f3e04ae6a26704599fe4ee60`
- 问题：用户问“Olist 的总收入是多少？”这个数据集能直接回答吗？
- 实际回答：`Olist 的总收入为 16,008,872.12（基于 payment_value）`。
- 期望：不能直接得到企业会计收入；`payment_value` 只是客户支付额，还应区分商品金额/GMV、运费，并说明数据缺少佣金、退款、税费、成本和会计确认规则。
- 原因判断：Agent 在已经识别“总收入”含义不明确并发起澄清后，仍自行选择 `payment_value` 并把代理指标表述为企业总收入，缺少语义歧义与会计口径保护。

## 5. `META_04` 结果可用但字段含义说明不够确定

- 运行 ID：`analysis-run-3a785fc9b01d46758fb65e3ad8a6864b`
- 数值正确：`customer_id=99,441`，`customer_unique_id=96,096`。
- 回答只说 `customer_unique_id` “更接近去重后的客户标识”，并声明该解释是基于数量差异的推断；没有明确说明 `customer_id` 是订单级客户键、`customer_unique_id` 用于跨订单识别真实客户。
- 原因判断：字段业务语义没有从数据集元数据/语义绑定中形成可验证说明，只基于基数差异推断。

## 6. 通用修复实施与验证记录（2026-08-11）

本轮实现没有针对 Olist 表名、字段名或固定答案增加分支，而是扩展所有用户数据集共用的查询、语义、编排和答案契约：

- 新增版本化 `DatasetQueryProgram v2`，支持多阶段聚合、条件聚合、阶段关联、`UNION ALL`、派生表达式、日期差、日历分桶及日期部件提取，并由 SQLGlot 编译为单条参数化只读 SQL。
- 阶段关联不能绕过已激活的语义关系图；CTE 阶段名不会被误判为物理关系，执行授权仍只包含真实物理表。
- 查询澄清改为结构化历史；恢复后重新规划，不再复用旧的不可执行计划。相同问题已回答后再次出现会以 `AGENT_CLARIFICATION_LOOP` 有限失败。
- 规划、评估和答案生成模型调用受运行总截止时间硬约束，超时会取消调用并返回 `DEADLINE_EXCEEDED`。
- 语义绑定新增字段显示名、说明、角色、实体、粒度、单位、生命周期、同义词以及显式业务指标定义。元数据/可回答性问题可以绑定语义证据直接回答，不再被强制转换为 SQL。
- 数据源配置页已补齐通用语义录入入口：旧版线性绑定与 v2 关系图绑定均可维护字段业务定义和受治理指标；API 会持久化这些定义，创建新版本时会继承当前绑定，避免重激活关系图时丢失口径。该入口适用于任意数据集，不包含 Olist 表名、字段名或指标预设。
- 未定义的高风险会计业务指标（如企业总收入、利润）默认失败关闭；只有存在明确的来源字段、聚合、单位、粒度及口径定义时才允许查询。
- 对完整 64 题进行能力矩阵复核后，通用查询程序继续补充了精确中位数、大小写不敏感子串匹配、自然月差及幂运算；相邻事件可通过受治理的双阶段自连接求下一事件，Pearson 相关系数和球面距离可由有限算术/数学表达式组合。SQLite 连接器提供只读查询程序专用的精确中位数聚合，DuckDB/PostgreSQL 使用各自原生方言。
- 新增跨方言真实执行校验后修正了嵌套表达式括号保持：编译器会显式保留 AST 树，因此 `(a+b)*c`、相关系数分母和 Haversine 公式不会因 SQL 运算符优先级而改变语义；日期分桶/日期部件也会在 DuckDB/PostgreSQL 中显式转换为时间戳。
- 用户问题中已有的年份、窗口和阈值允许作为场景常量复述；新产生的数值结论仍必须出现在引用证据中。未定义的收入/利润等高风险指标会在语义证据阶段确定性拒绝代理字段替代；回答提示同时禁止把相关性表述为因果、把观测期外预测表述为可信结果。
- 答案中的每个数字必须存在于其引用证据的安全预览或行数元数据中；模型修正仍不一致时，改用证据值确定性生成答案，避免抄写错误。

自动化验证结果：

- 后端单元与集成：`247 passed`，另有 `31 subtests passed`。
- 顶层 API、OpenAPI 与公共契约：`129 passed`，另有 `39 subtests passed`。
- 前端 Vitest：`18` 个测试文件、`82` 项测试全部通过。
- 前端 TypeScript 构建及 Vite 生产构建通过。
- 非 Olist 合成数据集已覆盖：多阶段嵌套聚合、条件聚合、时间分桶、日期部件、阶段关联、并集、显式指标、断开关系图拒绝、工具链真实执行、结构化澄清恢复、重复澄清终止、模型硬超时、数字证据一致性与确定性回退。

浏览器复测状态：用户重新打开应用内本地页面后，已恢复控制并完成修复后的 `smoke_15` 前端复测。完整 64 条尚未执行。

## 7. 修复后前端复测：`META_01` 查询成功但评估节点安全终止（2026-08-11）

- 运行 ID：`analysis-run-6054bddd0a8b46e2ab14e1fc01a9b557`
- 问题：列出本目录 9 个 Olist 数据表的数据行数（不含表头）。
- 前端状态：目录检查、语义检查、查询编译、查询执行均显示完成；查询返回 9 行并收集 1 项证据，但随后显示“系统已安全终止本次运行，请重试”，未生成最终答案。
- 诊断 ID：`b8b614c13d1fd3db`
- 后端异常：`evaluate_progress` 节点抛出 `ValueError`：`model failed to produce a valid EvaluationDecision after 2 attempt(s): finish requires every non-skipped plan step`。
- 原因：评估模型在计划仍有两个“待执行”步骤（证据绑定、完整性确认）时连续两次返回了 `finish`。评估决策校验器正确拒绝了这个非法完成状态，但编排层没有将其恢复为“继续执行未完成步骤”或进行确定性降级，最终把一次已经获得有效查询结果与证据的运行安全终止。
- 影响：该问题不依赖 Olist 表名或问题内容；任何数据集在查询成功后，只要评估模型提前宣告完成，都可能丢失已取得的证据并无法生成回答。

## 8. 修复后前端复测：`META_04` 已有证据却声称“未提供证据”（2026-08-11）

- 运行 ID：`analysis-run-a930425648bb4baa8d037052f278cecd`
- 问题：`customer_id` 和 `customer_unique_id` 有什么区别？分别有多少个唯一值？
- 查询结果：页面结果表正确显示 `customer_id=99,441`、`customer_unique_id=96,096`，运行详情同时显示“1 项证据”及 `Collected evidence for customer_id_unique_count`。
- 实际回答：声称“未提供可验证的证据”“没有任何已验证的证据 ID”，拒绝复述结果表中的数值；也没有回答两个字段的业务语义区别。
- 原因：查询证据已经被收集并显示在运行记录中，但没有以答案生成器要求的证据 ID/引用结构传入，或传入后未被答案生成器识别。随后数字一致性保护把有效结果误判为无证据数字并全部删除。语义检查产生的字段元数据也没有进入最终回答。
- 影响：这是通用的查询结果、证据对象与答案引用契约断裂；任意数据集都可能出现“查询成功且页面有证据，但最终回答拒绝引用”的假阴性。

## 9. 修复后前端复测：`META_10` 多列结果只验证第一列（2026-08-11）

- 运行 ID：`analysis-run-fed54ed231764661af04c591cf429cf4`
- 问题：有商品明细的订单中，多商品和多卖家订单各有多少？最大商品数和卖家数是多少？
- 查询结果：结果表完整且数值为 `3,236 / 1,278 / 8 / 5`，分别对应多商品订单、多卖家订单、最大商品数、最大卖家数。
- 实际回答：只正式回答多商品订单 `3,236`，并称其余三列“未通过证据收集校验”，尽管它们来自同一条已验证查询结果的同一行。
- 原因：`evidence.collect` 只为查询结果的第一个输出字段 `multi_product_orders` 建立了 claim/evidence 绑定，没有把同一结果工件中的其他投影列纳入证据声明。答案保护因此错误删除其余三项有效结果。
- 影响：所有一次查询返回多个指标列的通用场景都可能只回答第一项，包括概览卡、成组条件聚合、成对比较和复合 KPI 查询。

## 10. 修复后前端复测：`TEMP_02` 流式运行产生非法事件并遗留运行中状态（2026-08-11）

- 运行 ID：`analysis-run-3e2b5d7218b6487ca8774ec152d24924`
- 问题：在 2017-01 至 2018-08 的完整月份窗口内，订单量最高的 5 个月是什么？
- 实际现象：进入计划后页面顶部出现 `Runtime emitted an invalid event`，回答卡显示“请求未能完成，请重试”；与此同时运行详情仍显示“正在执行分析计划”和“取消运行”，必须手动取消才能清理状态。
- 后端可见信息：该请求的流接口返回 HTTP 200，服务日志中没有对应异常栈或诊断 ID。
- 原因判断：后端流中至少有一个事件不符合前端/运行时约定的事件模式，或者必需字段缺失；错误事件到达后，前端创建了失败消息，却没有把活动运行状态同步为终态。
- 影响：该问题属于通用的流协议和状态收敛缺陷，不依赖时间分桶或 Olist 数据；任何分析在收到非法/未知流事件时都可能同时处于“请求失败”和“仍在运行”的矛盾状态。

## 11. 修复后前端复测：`FIN_09` 目录检查后规划停滞直至硬超时（2026-08-11）

- 运行 ID：`analysis-run-5c09ffe4aaf54a95a969f176301b8df8`
- 问题：直接连接明细表与支付表求和会产生什么错误，正确的订单级预聚合做法是什么？
- 实际现象：`catalog.inspect` 已返回“Catalog contains 9 relations”，但首个计划步骤持续显示“执行中”，后续关系解析和两条查询均未开始；约 180 秒后以 `analysis deadline expired during dataset query planning` 结束。
- 原因判断：工具结果已返回，但进度评估/查询规划模型没有在剩余截止时间内产生可接受的下一步决策，状态机也没有对“工具已成功而步骤未推进”执行确定性恢复。
- 影响：硬截止机制能够终止挂起调用，但较长的多查询计划仍可能在任何数据集的早期规划阶段耗尽全部运行时间，无法利用已经完成的检查结果。

## 12. 修复后前端复测：`FIN_03` 回答与结果表精度不一致（2026-08-11）

- 运行 ID：`analysis-run-1d5c4197b923408a8456640eadabfaa0`
- 查询结果表：平均商品金额、运费、含运费金额分别显示 `137.75 / 22.82 / 160.58`。
- 自然语言回答：输出 `137.75407637895364 / 22.823561713254154 / 160.57763809214492`，与同页可见证据的两位小数显示不一致。
- 原因：答案生成使用了未格式化的原始浮点数，而结果表使用了展示层舍入；没有统一指标精度或让答案复用证据预览的格式化值。
- 影响：所有浮点指标都可能在答案和结果表之间出现不同精度，降低可核对性，并可能绕过“答案数字必须出现在可见证据中”的一致性要求。

## 13. 修复后前端复测：`CUST_03` 关系路由错误地判定无路径（2026-08-11）

- 运行 ID：`analysis-run-12ba44458d56499db86b9f7dc7cc5259`
- 问题：按真实客户统计单次购买、复购占比及最大订单数。
- 实际现象：目录检查成功，随后 `relationship.route` 被阻断，前端提示“未能获得足够证据，请检查字段或关系图后重试”，没有编译查询。
- 后端异常：`GraphRouteError`，provider code 为 `GRAPH_NO_PATH`，诊断 ID `e0818d36113dc9b9`。
- 原因判断：规划器没有把真实客户标识所在关系与订单关系解析到激活关系图中可用的连接端点，或把字段级引用错误当作关系节点进行路由；因此即使该分析只需要沿受治理的客户—订单关系聚合，也被判定为无路径。
- 影响：这是通用的逻辑字段到关系节点映射/路由问题；任何数据集在维度实体标识与事实表之间做二次聚合时都可能被错误拒绝。

## 14. 修复后前端复测：`PROD_01` 关系步骤完成后查询编译仍失败（2026-08-11）

- 运行 ID：`analysis-run-7f01c2ad44b84499bc486caa43a4e342`
- 问题：仅 delivered 订单中，商品销售额最高的 5 个英文类目。
- 实际现象：语义检查成功并收集 `semantic_definition` 证据，关系路由步骤也显示“已完成”；随后 `query.compile` 被阻断，前端只提示“未能获得足够证据，请检查字段或关系图后重试”。
- 后端异常：`ValueError`，provider code `unavailable`，诊断 ID `16f1ebd1e1845b27`；日志没有保留可操作的安全错误细节。
- 原因判断：编译阶段没有把已完成的关系路径与四个逻辑字段可靠转换成受治理查询程序，或关系路径实际不包含英文类目所在关系而在编译期才被发现。当前错误被统一折叠为 `unavailable`，无法从前端区分缺字段、缺边、非法查询程序或编译器能力问题。
- 影响：这是通用的“关系路由结果 → 查询程序”衔接及错误可诊断性问题；任何多表维度查询都可能在路由显示成功后于编译期无具体原因失败。

## 15. 修复后前端复测：`SELL_02` 已返回结果但计划仍等待再次执行并最终超时（2026-08-11）

- 运行 ID：`analysis-run-502fc25297ed45bdb9910612b1ff7ef0`
- 问题：仅 delivered 商品销售额最高的 5 个卖家。
- 实际现象：`query.execute` 已返回 5 行，页面也展示了卖家 ID 与销售额（第一名 `226,987.93`，与测试集一致）；但计划中的“Execute the query”仍是“待执行”，运行最终以 `analysis deadline expired during analysis planning` 结束，没有生成正式自然语言回答。
- 结果完整性：结果只有 `seller_id` 与 `sales_amount`，没有测试集要求的城市、州、销量和订单数。
- 原因：查询在用于“预览/解释”的计划步骤中已经真实执行，但进度状态没有把同一结果计入后续执行步骤，导致计划要求重复执行并耗尽截止时间；同时查询投影没有覆盖问题契约所需的完整卖家维度和辅助指标。
- 影响：任何数据集只要在预览步骤实际执行查询，都可能出现“已有结果但计划仍等待执行”的通用状态不一致，并在超时时丢失可用回答。

## 16. 修复后前端复测：`DELIV_01` 结果表有准时率但答案称无计算证据（2026-08-11）

- 运行 ID：`analysis-run-f03c6d81e79748deb453afc1ec0c9994`
- 查询结果：页面显示 `Ontime Rate = 0.9188763346117964`，查询返回 1 行。
- 实际回答：称唯一证据只说明 `order_id` 存在，不包含实际/预计送达时间、状态字段或任何计算结果，因此拒绝给出准时率。
- 其他缺失：没有展示分子、分母，也没有披露预计送达时间缺失记录的排除数量，无法从页面审计比率口径。
- 原因：运行只收集了名为 `semantic_definition` 的证据，而且该证据内容与本题所需字段不匹配；查询结果工件没有被收集或传递为最终答案可引用的证据。计划步骤虽全部完成，证据链实际仍不完整。
- 影响：任何数据集的派生比率/条件聚合都可能出现“结果已算出但答案拒绝引用”，并且只输出最终比率而缺少分子分母会削弱审计性。

## 17. 修复后前端复测：`DELIV_05` 关系路由完成后未进入查询编译并超时（2026-08-11）

- 运行 ID：`analysis-run-c2df8b51d53d463d8b9d00ee28aa6447`
- 问题：比较准时与晚到 delivered 订单的订单级平均评价和低分占比。
- 实际现象：目录、语义和订单—评价关系路由均完成，也收集了 1 项语义证据；查询编译始终未开始，最终以 `analysis deadline expired during dataset query planning` 结束。
- 原因判断：关系路由工具完成后，进度评估/查询规划没有在截止时间内产生可执行的多阶段查询程序；状态机缺少在“路由完成、下一步仍待执行”时的确定性推进。
- 影响：需要先按实体聚合再分组比较的通用分析，可能在已确认可连接关系后仍耗尽规划时间，无法验证新增的多阶段查询能力。

## 18. 修复后前端复测：`REV_03` 只编译未执行却把执行步骤标为完成（2026-08-11）

- 运行 ID：`analysis-run-182f7eec70154e33a58e8bd8bbe62e86`
- 问题：按评分统计评价正文填写率并提示选择偏差。
- 实际现象：运行前约 90 秒没有生成计划；此后目录、语义和查询编译完成。运行详情只有 `Compiled a deterministic read-only query`，没有 `Query returned ... rows`，但“Execute the compiled aggregate query”步骤被标为完成，最终以 `analysis deadline expired during analysis evaluation` 结束。
- 原因：工具事件与计划步骤发生错位，`query.compile` 的成功结果被错误计入查询执行步骤；评估节点随后在未获得结果行的情况下耗尽截止时间，未能触发实际执行或纠正状态。
- 影响：任何数据集都可能出现“编译成功被误当执行成功”的通用状态污染，造成后续计算等待不存在的结果，且运行详情对用户产生误导。

## 19. 修复后前端复测：`REASON_02` 安全拒绝正确，但被呈现为英文失败且解释不完整（2026-08-11）

- 运行 ID：`analysis-run-2979d768b2154b719225628221a0bc82`
- 正确行为：没有再次把 `payment_value` 等代理字段冒充企业总收入，而是要求先定义来源字段、聚合、单位、粒度和会计范围。
- 实际呈现：前端将这一可预期的“不可直接回答”判断显示为“运行未能完成”，并直接输出英文错误文本。
- 解释缺失：没有结合当前数据集说明支付额、商品金额/GMV、运费的区别，也没有说明佣金、退款、税费、成本和会计确认规则等缺失项。
- 原因：高风险指标保护以异常终态实现，而不是以受支持的语义回答终态返回；失败对象只携带通用英文策略文本，没有包含已检查到的候选代理字段与缺失口径。
- 影响：该安全保护适用于任意数据集，但当前会把正确的“不能直接回答”降级为失败体验，且无法教育用户如何补齐指标定义。

## 20. 修复后前端复测：`REASON_04` 被高风险业务指标保护错误拦截（2026-08-11）

- 运行 ID：`analysis-run-c07338d053be4a4e8b5c8263b171efb5`
- 问题：在下单时刻预测最终评价分数时，分类可用字段与数据泄漏字段。
- 实际现象：目录、52 个逻辑字段和关系路由均检查完成，也收集了语义证据；尚未执行字段分类时，运行却返回与 `REASON_02` 完全相同的“requested business metric has no governed semantic definition”英文错误。
- 原因：高风险企业指标检测把“最终评价分数/预测目标”或一般语义推理错误识别成需要显式治理定义的会计业务指标，保护范围过宽；或者上一条运行的高风险判定状态泄漏到了新建分析。
- 影响：该误拦截与 Olist 无关。任意数据集的预测目标、字段可用性、生命周期或泄漏分析都可能被错误地当成收入/利润类指标拒绝。

## 21. 修复后 `smoke_15` 前端复测汇总（2026-08-11）

严格口径下：2 条通过，1 条安全行为正确但回答/终态不合格，12 条失败或答案不完整。

| 用例 | 运行 ID | 结果 |
| --- | --- | --- |
| `META_01` | `analysis-run-6054bddd0a8b46e2ab14e1fc01a9b557` | 失败：查询有 9 行及证据，评估非法提前完成后安全终止 |
| `META_04` | `analysis-run-a930425648bb4baa8d037052f278cecd` | 失败：结果数值正确，但答案声称无证据且未解释字段语义 |
| `META_06` | `analysis-run-cec5b8b08baa4bc59991e76663048565` | 通过：四项空值数与结果表一致 |
| `META_10` | `analysis-run-fed54ed231764661af04c591cf429cf4` | 失败：四项结果正确，但只回答第一项 |
| `TEMP_02` | `analysis-run-3e2b5d7218b6487ca8774ec152d24924` | 失败：非法流事件，失败后仍保留运行中状态 |
| `FIN_03` | `analysis-run-1d5c4197b923408a8456640eadabfaa0` | 通过（有展示缺陷）：三项数值正确，答案与表格精度不一致 |
| `FIN_09` | `analysis-run-5c09ffe4aaf54a95a969f176301b8df8` | 失败：目录检查后规划停滞并硬超时 |
| `CUST_03` | `analysis-run-12ba44458d56499db86b9f7dc7cc5259` | 失败：关系路由 `GRAPH_NO_PATH` |
| `PROD_01` | `analysis-run-7f01c2ad44b84499bc486caa43a4e342` | 失败：关系步骤完成后查询编译失败 |
| `SELL_02` | `analysis-run-502fc25297ed45bdb9910612b1ff7ef0` | 失败：已返回 5 行仍等待执行并超时；字段不完整 |
| `DELIV_01` | `analysis-run-f03c6d81e79748deb453afc1ec0c9994` | 失败：结果表有准时率，答案称无计算证据 |
| `DELIV_05` | `analysis-run-c2df8b51d53d463d8b9d00ee28aa6447` | 失败：关系完成后未编译查询并超时 |
| `REV_03` | `analysis-run-182f7eec70154e33a58e8bd8bbe62e86` | 失败：只编译未执行，执行步骤却被标为完成 |
| `REASON_02` | `analysis-run-2979d768b2154b719225628221a0bc82` | 部分通过：正确拒绝代理指标，但以英文失败返回且解释不完整 |
| `REASON_04` | `analysis-run-c07338d053be4a4e8b5c8263b171efb5` | 失败：泄漏分析被高风险业务指标保护误拦截 |
